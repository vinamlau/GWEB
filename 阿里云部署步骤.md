# 🚀 阿里云部署指南

## 第一步：上传代码到 GitHub

### 1.1 创建 GitHub 仓库

1. 访问 https://github.com/new
2. 仓库名称：`gweb`
3. 设置为 **Public** 或 **Private**（您决定）
4. **不要** 勾选 "Add a README file"
5. 点击 "Create repository"

### 1.2 上传代码

在您的终端执行以下命令：

```bash
# 进入项目目录
cd /Users/victor/Documents/trae_projects/GWEB

# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit - GWEB 项目"

# 重命名分支为 main
git branch -M main

# 添加远程仓库（替换 YOUR_USERNAME 为您的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/gweb.git

# 推送到 GitHub
git push -u origin main
```

**注意**：如果出现错误，请告诉我具体的错误信息。

---

## 第二步：准备阿里云服务器

### 2.1 配置安全组

登录阿里云控制台：

1. 访问 https://ecs.console.aliyun.com/
2. 找到您的实例
3. 点击实例 ID
4. 点击 **安全组** 标签
5. 点击 **配置规则**
6. 添加入站规则：
   - 端口 22 (SSH) - 授权对象：0.0.0.0/0
   - 端口 80 (HTTP) - 授权对象：0.0.0.0/0
   - 端口 443 (HTTPS) - 授权对象：0.0.0.0/0

### 2.2 连接服务器

在您的终端执行：

```bash
# 替换为您的服务器 IP
ssh root@your-server-ip
```

输入您的服务器密码（输入时不会显示）。

---

## 第三步：在服务器上执行部署

成功登录服务器后，执行以下命令：

```bash
# 1. 更新系统
apt update && apt upgrade -y

# 2. 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# 3. 安装 Git
apt install -y git

# 4. 安装 Nginx
apt install -y nginx

# 5. 安装 PM2
npm install -g pm2

# 6. 克隆项目
cd /var/www
git clone https://github.com/YOUR_USERNAME/gweb.git
cd gweb

# 7. 安装前端依赖并构建
npm install
npm run build

# 8. 安装后端依赖
cd server
npm install

# 9. 创建环境配置
cat > .env << EOF
NODE_ENV=production
PORT=3001
JWT_SECRET=$(openssl rand -hex 32)
EOF

# 10. 配置 Nginx
cd /var/www/gweb
cat > /etc/nginx/sites-available/gweb << 'EOF'
server {
    listen 80;
    server_name _;

    # 前端静态文件
    location / {
        root /var/www/gweb/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 上传文件
    location /uploads {
        alias /var/www/gweb/server/uploads;
        expires 30d;
    }
}
EOF

# 启用 Nginx 配置
ln -s /etc/nginx/sites-available/gweb /etc/nginx/sites-enabled/gweb
nginx -t
systemctl restart nginx
systemctl enable nginx

# 11. 启动后端服务
cd /var/www/gweb/server
pm2 start server-sqlite.js --name gweb-api
pm2 save
pm2 startup | tail -1 | bash

echo "✅ 部署完成！"
echo ""
echo "访问地址："
echo "  前台：http://your-server-ip"
echo "  后台：http://your-server-ip/admin/login"
echo ""
echo "管理员账户：admin@example.com / admin123456"
```

---

## 第四步：验证部署

### 4.1 访问网站

在浏览器中打开：

- 前台：`http://your-server-ip`
- 后台：`http://your-server-ip/admin/login`

### 4.2 登录后台

使用以下账户登录：

- 邮箱：admin@example.com
- 密码：admin123456

### 4.3 测试功能

- ✅ 查看仪表盘
- ✅ 创建文章
- ✅ 上传图片
- ✅ 管理用户

---

## 第五步：配置域名（可选）

如果您有域名：

### 5.1 DNS 解析

在您的域名服务商处添加 A 记录：

- 主机记录：@
- 记录类型：A
- 记录值：your-server-ip
- TTL：10 分钟

### 5.2 修改 Nginx 配置

在服务器上执行：

```bash
# 编辑 Nginx 配置
nano /etc/nginx/sites-available/gweb

# 修改 server_name 为您的域名
# server_name your-domain.com www.your-domain.com;

# 测试并重启 Nginx
nginx -t
systemctl restart nginx
```

### 5.3 安装 SSL 证书

```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d your-domain.com -d www.your-domain.com
```

---

## 常用运维命令

### 查看服务状态

```bash
pm2 status
```

### 查看日志

```bash
pm2 logs gweb-api
```

### 重启服务

```bash
pm2 restart gweb-api
```

### 更新代码

```bash
cd /var/www/gweb
git pull
npm install
npm run build
cd server
npm install
pm2 restart gweb-api
```

---

## 遇到问题？

### 常见问题解决

**1. 无法访问网站**

```bash
# 检查 Nginx 状态
systemctl status nginx

# 检查防火墙
ufw status
```

**2. 后端服务未运行**

```bash
# 查看 PM2 状态
pm2 status

# 重启服务
pm2 restart gweb-api
```

**3. 端口被占用**

```bash
# 查看端口占用
lsof -i:3001
lsof -i:80

# 终止进程
kill -9 <PID>
```

---

## 下一步

部署成功后，您可以：

1. 修改管理员密码
2. 创建新的管理员账户
3. 上传您的内容
4. 配置域名和 SSL
5. 设置自动备份

---

**需要帮助？** 请告诉我部署过程中遇到的任何问题！
