# 首页编辑说明

## 问题原因

之前首页（Home.tsx）是**硬编码的静态内容**，不会从后台 API 获取数据，所以后台编辑的内容无法同步到前台。

## 已修复

现在首页会：

1. **自动从 API 获取内容** - 访问 `/api/pages/home`
2. **动态显示** - 如果后台编辑了内容，显示后台内容；否则显示默认设计
3. **实时更新** - 后台保存后自动刷新显示

## 如何使用

### 方式 1：使用后台编辑（推荐）

1. 登录后台：http://gcore.xin/admin/login
2. 进入 **页面管理** → **首页**
3. 点击 **编辑** 按钮
4. 在富文本编辑器中编辑内容
5. 点击 **保存**

保存后，前台会自动显示新内容（需要刷新浏览器）

### 方式 2：直接修改代码

如果要修改默认设计（当后台没有编辑内容时显示的设计）：

1. 编辑文件：`src/pages/Home.tsx`
2. 修改默认内容
3. 重新构建并部署

## 编辑内容格式

后台编辑支持 HTML 格式，可以包含：

```html
<div class="custom-home">
  <h1>欢迎来到集团公司</h1>
  <p>专业的服务提供商</p>

  <div class="features">
    <h2>我们的优势</h2>
    <ul>
      <li>技术创新</li>
      <li>专业服务</li>
      <li>全球布局</li>
    </ul>
  </div>
</div>
```

## 样式说明

首页内容会自动应用以下样式：

- 标题（h1, h2, h3）会自动美化
- 段落文字会有合适的行高和颜色
- 图片会自动适配宽度
- 列表会有正确的缩进

## 清除缓存

如果编辑后看不到变化：

1. **强制刷新浏览器**
   - Windows: `Ctrl + Shift + R`
   - Mac: `Cmd + Shift + R`

2. **清除浏览器缓存**
   - Chrome: 设置 → 隐私和安全 → 清除浏览数据
   - Safari: 开发 → 清空缓存

## 其他页面

同样的功能也适用于：

- **关于我们** (`/about`)
- **联系我们** (`/contact`)
- **新闻动态列表页** (`/news`)
- **业务板块页面** (`/edge-computing`, `/payment-finance`, `/ecommerce`)

所有这些页面现在都会从后台 API 获取内容！

## 测试步骤

1. 登录后台管理
2. 进入页面管理
3. 编辑首页内容
4. 保存
5. 访问 http://gcore.xin
6. 刷新页面（Ctrl+Shift+R）
7. 查看新内容

## 技术实现

- **前端**: React + TypeScript
- **API**: Express + SQLite
- **实时更新**: CustomEvent ('pageUpdated')
- **缓存控制**: Nginx 配置 no-cache

## 更新日期

2024-04-05 - 修复首页内容同步问题
