# 📊 阿里云部署状态检查指南

## 快速检查方法

### 方法一：访问网站（最简单）

在浏览器中打开：

- **前台**：http://172.16.61.129
- **后台**：http://172.16.61.129/admin/login

如果能正常显示页面，说明部署成功！

---

## 详细检查步骤

### 第一步：连接服务器

```bash
# SSH 登录阿里云服务器
ssh root@172.16.61.129
```

输入您的服务器密码。

---

### 第二步：检查服务状态

#### 1. 检查 Nginx 状态

```bash
systemctl status nginx
```

**✅ 成功标志：**

```
● nginx.service - A high performance web server and a reverse proxy server
   Loaded: loaded (/lib/systemd/system/nginx.service; enabled)
   Active: active (running)
```

**❌ 如果未运行：**

```bash
sudo systemctl start nginx
```

#### 2. 检查 PM2 后端服务

```bash
pm2 status
```

**✅ 成功标志：**

```
┌────┬───────────┬─────────────┬─────────┬─────────┬──────────┬────────┬──────┬───────────┬──────────┬──────────┐
│ id │ name      │ namespace   │ status  │ cpu     │ mem      │ …      │ …    │ …         │ …        │ …        │
├────┼───────────┼─────────────┼─────────┼─────────┼──────────┼────────┼──────┼───────────┼──────────┼──────────┤
│ 0  │ gweb-api  │ default     │ online  │ 0%      │ 50mb     │        │      │           │          │          │
└────┴───────────┴─────────────┴─────────┴─────────┴──────────┴────────┴──────┴───────────┴──────────┴──────────┘
```

**❌ 如果未运行：**

```bash
cd /var/www/gweb/server
pm2 start server-sqlite.js --name gweb-api
pm2 save
```

#### 3. 检查端口监听

```bash
# 检查 80 端口（Nginx）
sudo lsof -i:80

# 检查 3001 端口（后端）
sudo lsof -i:3001
```

**✅ 成功标志：**

- 80 端口有 nginx 进程
- 3001 端口有 node 进程

#### 4. 检查项目文件

```bash
cd /var/www/gweb
ls -la
```

**✅ 应该看到：**

```
dist/          # 前端构建文件
server/        # 后端代码
node_modules/  # 前端依赖
package.json
```

#### 5. 检查数据库

```bash
cd /var/www/gweb/server
ls -la data.db
```

**✅ 成功标志：**

- data.db 文件存在

---

### 第三步：功能测试

#### 1. 测试前端页面

访问以下页面：

- http://172.16.61.129（首页）
- http://172.16.61.129/about（关于我们）
- http://172.16.61.129/admin/login（后台登录）

#### 2. 测试后台登录

1. 访问：http://172.16.61.129/admin/login
2. 使用账户：
   - 邮箱：admin@example.com
   - 密码：admin123456
3. 成功登录后，查看仪表盘

#### 3. 测试 API 接口

```bash
# 在服务器上测试
curl http://localhost:3001/api/health

# 应该返回：
# {"status":"ok","environment":"production","database":"sqlite",...}
```

#### 4. 测试创建内容

在后台管理中：

- ✅ 创建一篇新文章
- ✅ 上传一张图片
- ✅ 修改站点配置
- ✅ 保存后检查是否成功

---

## 常见问题排查

### 问题 1：无法访问网站

**检查防火墙：**

```bash
sudo ufw status
```

**如果防火墙开启，放行端口：**

```bash
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 22/tcp
```

**检查 Nginx 配置：**

```bash
sudo nginx -t
sudo systemctl restart nginx
```

### 问题 2：后台无法登录（401 错误）

**检查后端服务：**

```bash
pm2 logs gweb-api
```

**重启后端：**

```bash
pm2 restart gweb-api
```

### 问题 3：API 请求失败

**检查 Nginx 代理配置：**

```bash
cat /etc/nginx/sites-available/gweb
```

**确保有 /api 代理配置：**

```nginx
location /api {
    proxy_pass http://localhost:3001;
    # ... 其他配置
}
```

### 问题 4：页面空白

**检查浏览器控制台错误（F12）**

**检查前端文件：**

```bash
ls -la /var/www/gweb/dist
```

**重新构建前端：**

```bash
cd /var/www/gweb
npm run build
```

---

## 性能监控

### 查看系统资源

```bash
# CPU 和内存使用
htop

# 磁盘空间
df -h

# 内存使用
free -h
```

### 查看应用日志

```bash
# PM2 日志
pm2 logs gweb-api

# Nginx 访问日志
tail -f /var/log/nginx/access.log

# Nginx 错误日志
tail -f /var/log/nginx/error.log
```

---

## 安全加固建议

### 1. 修改默认密码

登录后台后，立即修改管理员密码。

### 2. 配置防火墙

```bash
# 安装 ufw
apt install -y ufw

# 放行必要端口
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp

# 启用防火墙
ufw enable
```

### 3. 安装 SSL 证书

```bash
# 安装 Certbot
apt install -y certbot python3-certbot-nginx

# 申请证书
certbot --nginx -d gcore.xin -d www.gcore.xin
```

### 4. 定期备份

```bash
# 创建备份脚本
cat > /root/backup.sh << 'EOF'
#!/bin/bash
DATE=$(date +%Y%m%d)
cp /var/www/gweb/server/data.db /backup/data-$DATE.db
tar -czf /backup/uploads-$DATE.tar.gz /var/www/gweb/server/uploads
EOF

chmod +x /root/backup.sh

# 添加定时任务（每天凌晨 2 点）
crontab -e
# 添加：0 2 * * * /root/backup.sh
```

---

## 部署成功检查清单

- [ ] Nginx 运行正常（systemctl status nginx）
- [ ] PM2 服务在线（pm2 status）
- [ ] 80 端口监听正常
- [ ] 3001 端口监听正常
- [ ] 首页可以访问
- [ ] 后台可以登录
- [ ] 可以创建文章
- [ ] 可以上传图片
- [ ] API 接口正常
- [ ] 数据库文件存在

---

## 快速诊断脚本

创建一个快速检查脚本：

```bash
cat > /root/check-deploy.sh << 'EOF'
#!/bin/bash

echo "========================================="
echo "🔍 GWEB 部署状态检查"
echo "========================================="

echo ""
echo "1️⃣ Nginx 状态："
systemctl is-active nginx

echo ""
echo "2️⃣ PM2 状态："
pm2 status gweb-api

echo ""
echo "3️⃣ 端口监听："
echo "  80 端口：$(sudo lsof -i:80 | grep LISTEN | wc -l) 个进程"
echo "  3001 端口：$(sudo lsof -i:3001 | grep LISTEN | wc -l) 个进程"

echo ""
echo "4️⃣ 项目文件："
ls -la /var/www/gweb/ | head -10

echo ""
echo "5️⃣ 数据库："
ls -lh /var/www/gweb/server/data.db

echo ""
echo "6️⃣ 磁盘空间："
df -h / | tail -1

echo ""
echo "========================================="
EOF

chmod +x /root/check-deploy.sh
```

执行检查：

```bash
bash /root/check-deploy.sh
```

---

## 需要帮助？

如果遇到任何问题，请提供以下信息：

1. **错误截图或日志**
2. **执行 `bash /root/check-deploy.sh` 的输出**
3. **浏览器控制台错误（F12）**

---

**祝您部署顺利！** 🎉
